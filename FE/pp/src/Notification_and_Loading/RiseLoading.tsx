import { RiseLoader } from "react-spinners";
import { globalColors } from "../styles/Colors";

export const RiseLoading = () => {
  return <RiseLoader color={globalColors.navbarcolor} size={20} />;
};
